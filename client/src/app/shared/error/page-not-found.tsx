import React from 'react';

import { Alert } from 'reactstrap';

const PageNotFound = () => {
  return (
    <div>
      <Alert color="danger">Halaman tidak ada.</Alert>
    </div>
  );
};

export default PageNotFound;
